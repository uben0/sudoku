[workspace]
resolver = "3"
members = ["sudoku", "sudoku_cli", "sudoku_wasm"]

[workspace.dependencies]
rand = { version = "0.10.0", default-features = false }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
overflow-checks = false
