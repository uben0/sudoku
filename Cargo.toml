[workspace]
resolver = "3"
members = ["sudoku", "sudoku-cli", "sudoku-web"]

[workspace.dependencies]
rand = { version = "0.10.0", default-features = false, features = ["std", "std_rng", "thread_rng"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
overflow-checks = false
