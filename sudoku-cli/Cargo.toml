[package]
name = "sudoku-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.58", features = ["derive"] }
sudoku = { path = "../sudoku" }
rand = { workspace = true }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
overflow-checks = false
